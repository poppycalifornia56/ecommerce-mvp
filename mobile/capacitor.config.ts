import { CapacitorConfig } from '@capacitor/core';

const config: CapacitorConfig = {
  appId: 'com.example.eshop',
  appName: 'E-Shop',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
